theme = dracula

font-family = JetBrainsMono Nerd Font

font-feature = -calt
font-feature = -dlig
font-feature = -liga

font-size = {{ terminal_font_size | default(10) }}

confirm-close-surface = false

window-decoration = none
